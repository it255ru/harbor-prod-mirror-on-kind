# Backlog: Harbor active-active на KinD — прод-зеркало (форк)

**Статус: форк создан 2026-09-25 — версии закреплены (P0), реализация ещё не начата.** Родитель — `harbor-active-active-on-kind` @ `7279079` (весь его backlog, включая закрытые Phase 0–4/H5.x/Phase 6 ниже, унаследован как история; сам он — форк `harbor-on-kind` @ `b65df71`). Полная история сохранена в git.

**Цель форка:** как можно точнее воспроизвести конкретный боевой стенд, а не общую HA-схему родителя: Harbor `2.14.3`, Redis Sentinel (в родителе уже так), HAProxy + Keepalived вместо MetalLB/ingress-nginx на входе. Расхождение с родителем было отмечено ещё в его решении D7: «там общий адрес, вероятно, держит keepalived, в лаборатории — ClusterIP Service / MetalLB» — этот форк его устраняет.

## Новые решения этого форка (2026-09-25)

- **D11 — HAProxy + Keepalived заменяют Infra LB.** MetalLB и ingress-nginx убираются целиком; Keepalived держит плавающий VIP (VRRP) на `lb`-нодах, HAProxy за ним балансирует HTTPS-трафик на Harbor. Решение пользователя.
- **D12 — Harbor `2.14.3` / chart `1.18.3`, официальный образ Docker Hub.** Прод использует вендорскую/внутреннюю сборку `v2.14.3-fa517e2a` — недоступна из этой сессии (не найдена ни на Docker Hub, ни в официальных релизах goharbor). Решение пользователя: заменить на публичный `v2.14.3`. Если вендорская сборка станет доступна — переключить и перезакрепить digest.
- **D13 — внешний PostgreSQL понижен до `15.19-alpine3.24`** (у родителя — `18.6-alpine3.24`). Chart `1.18.3` документирует встроенный PostgreSQL `15.12`; решение пользователя — не полагаться на неподтверждённую совместимость с PG 18, взять последний патч той же мажорной ветки 15.x (стиль пина — как у родителя: alpine3.24). Тянет пересборку образа `hack/ha/patroni` (Phase P4).
- **D14 — образ Keepalived: собственная минимальная сборка (Alpine + `apk add keepalived`), не сторонний образ.** Решение автора плана (не пользователя) — открыто к пересмотру. Причина: тот же урок, что и с MinIO у родителя (D4a) — сторонние образы `keepalived` на Docker Hub не от вендора инфраструктуры, могут исчезнуть или перестать обновляться; собственная сборка по образцу `hack/ha/patroni` контролируема и закрепляется по digest базового слоя.
- **Patroni/Consul, Redis/Sentinel, S3/Garage — без изменений**, схема и версии как у родителя (решение пользователя: «та же схема»).

## Закреплённые версии (изменения относительно родителя)

| Компонент | У родителя | Здесь |
|---|---|---|
| Harbor chart / app | `1.19.2` / `2.15.2` | **`1.18.3` / `2.14.3`** |
| PostgreSQL (внешний) | `18.6-alpine3.24` | **`15.19-alpine3.24`** |
| Infra LB | MetalLB `0.16.1` + ingress-nginx `4.15.1` | **HAProxy `3.4.4-alpine3.24` (тот же пин, что у Harbor LB) + Keepalived (своя сборка; версия Alpine и keepalived закрепляются при реализации P2)** |

Digest'ы (проверены 2026-09-25 — `docker pull` образа по тегу + digest из локального реестра):

| Образ | Digest |
|---|---|
| `goharbor/harbor-core:v2.14.3` | `sha256:a30e5a8be3d94b6485e7fdd4ed7fdf9e9724ee0a6d103b3804aacf6784ee358e` |
| `goharbor/harbor-portal:v2.14.3` | `sha256:2556b6c7dd832bf22ed8b177245fa5fbcd70255959e69c5d0fbe2153f4bd2243` |
| `goharbor/harbor-jobservice:v2.14.3` | `sha256:e2b0298e894d725d68954b786c61c5dd607114f6129534756c8f7985124c07a6` |
| `goharbor/harbor-registryctl:v2.14.3` | `sha256:ddf6bb429eb6b5a3db3e98bfe6ab3dd2567ebb35547836d87fc54ddacebfac8d` |
| `goharbor/registry-photon:v2.14.3` | `sha256:6533fc396cbce57131053faec55e1bd1da8b92aed318410c203ff1c7a9b910ab` |
| `goharbor/trivy-adapter-photon:v2.14.3` | `sha256:5c6f7162804cd94d41672a694579f136ccc9730d024dda9ad24e0f42bdae6f36` |
| `postgres:15.19-alpine3.24` | `sha256:f7d23353e1b15400d22ebe31189f4d314b87a4c129cc400c8c2d8d4ca127bf81` |

## Известное препятствие, найденное до начала реализации (2026-09-25)

Chart `1.18.3` **не поддерживает** `core.livenessProbe`/`readinessProbe` и `jobservice.livenessProbe`/`readinessProbe` как values — в шаблонах эти поля частично зашиты (core: `failureThreshold: 2`, без `timeoutSeconds`; jobservice: `initialDelaySeconds: 300`, без остального). Фикс родителя из H4.7 (`timeoutSeconds: 5, failureThreshold: 6` в `hack/config/harbor-ha.yaml`, чтобы core/jobservice не перезапускались при отказе Redis) в этой версии чарта через `values.yaml` не задать — будет молча проигнорирован. Переносится в `hack/helm-postrender.py` (Phase P3); без этого нужно заново подтвердить тестом `hack/tests/h47-role-failure.sh redis`, что рестартов не будет.

## План реализации форка

- [x] **P0** Форк создан (`git clone` с полной историей из `harbor-active-active-on-kind` @ `7279079`, `origin` отвязан), decisions D11–D14 приняты, версии и digest'ы закреплены, найдено структурное препятствие с пробами (2026-09-25).
- [ ] **P1** Ребрендинг документации (`README.md`, `CLAUDE.md`, `AGENTS.md`) под новый стек; `hack/images.txt` — заменить строки MetalLB/ingress-nginx на Keepalived (когда выбран образ) и HAProxy для нового Infra LB, обновить PostgreSQL/Harbor.
- [ ] **P2** Заменить Infra LB: убрать `make infra-lb`/MetalLB/ingress-nginx, добавить `hack/ha/keepalived.yaml` (VRRP VIP на `lb`-нодах) + HAProxy-фронтенд для HTTPS Harbor; решить (реализационно, не пользователем), объединять ли с существующим Harbor LB (PG/Redis) в один под или разносить.
- [ ] **P3** Понизить Harbor до chart `1.18.3`/app `2.14.3`: обновить `hack/config/harbor-ha.yaml` (digest'ы, убрать нерабочие ключи проб), перенести relaxed-liveness фикс в `hack/helm-postrender.py`.
- [ ] **P4** Понизить внешний PostgreSQL до `15.19-alpine3.24`: пересобрать `hack/ha/patroni` (Dockerfile `FROM`), сбросить Secret+PVC, `make postgres`.
- [ ] **P5** Полная пересборка с нуля + `make verify` + `hack/tests/` (особый акцент на `h47 redis` — фикс проб; `h47 pg` — PostgreSQL 15).
- [ ] **P6** Документация: `docs/stand-topology.md`, `docs/verification-runbook.md`, финальная сверка `backlog.md`.

---

# Унаследовано от родителя (`harbor-active-active-on-kind`, до фикса выше)

**Цель:** Harbor в active-active — несколько реплик core / portal / registry / jobservice (и, возможно, trivy) за ingress, с общими внешними PostgreSQL, Redis (Valkey) и S3-совместимым хранилищем. Потеря любой одной реплики не должна прерывать push/pull образов и OCI-чартов.

## Целевая архитектура (требование пользователя)

Источники (2026-09-24): таблица узлов «Harbor (compute)» и архитектурная диаграмма «Harbor / Nexus / Consul / Ceph». Назначение: Docker- и Helm-репозитории для ВМ клиентов в PVE-кластере compute; Harbor **проксирует внешние репозитории** (выход в Интернет — стрелка от «Harbor app») и **хранит локальные** (proxy-cache проекты + обычные проекты).

Что на диаграмме куда ходит:

| Поток | Откуда → куда | Заметка |
|-------|---------------|---------|
| Вход | администраторы и клиенты → **Infra LB** → Harbor (и Nexus) | Infra LB общий для Harbor и Nexus и стоит вне блока Harbor |
| Данные приложения | Harbor app → **Harbor LB** → Postgres Cluster / Redis Cluster | Harbor LB — балансировщик *перед PG и Redis*, а не вход в Harbor |
| Блобы registry | Harbor app → Ceph S3 («Данные») | Ceph internal (№14), S3 API |
| Бэкапы | Postgres Cluster и Redis Cluster → Ceph S3 («Бэкапы») | плюс «Выгрузка бэкапов во внутренний контур» |
| Состояние PG-кластера | Postgres Cluster → Consul (green) («Cluster state») | по стрелке — вероятно, DCS для Patroni (подтвердить) |
| Метрики | Prometheus (green) → Consul (green); «Выгрузка метрик во внутренний контур» | |
| Интернет | Harbor app → Интернет | proxy-cache |

Решением пользователя (D8) строки про бэкапы и метрики в лабораторию не входят — оставлены как часть исходной схемы.

Соответствие таблице узлов: `hb-app-01/02` = Harbor app (жёлтые), `hb-lb-01/02` = Harbor LB (зелёные), `hb-pg-01/02` = Postgres Cluster, `hb-redis-01..03` = Redis Cluster. Цвета на обеих схемах совпадают; легенды нет — смысл (вероятно, зоны/контуры) уточнить (D9).

Вне блока Harbor, но на диаграмме: **Nexus** (proxy, repo, LB, собственный Postgres Cluster), второй Consul/Prometheus («yellow»). Входит ли Nexus в лабораторию — D10 (по умолчанию нет: цель — Harbor).

**Что это меняет в первой версии этого раздела:**
- `hb-lb` — не входная точка Harbor, а балансировщик перед PG/Redis; вход обеспечивает Infra LB. Отсюда `database.external.host` = адрес Harbor LB, проблема «chart принимает один host:port» снимается.
- D4 закрыт по смыслу: блобы в S3 (Ceph RGW). В лаборатории S3-стенд-ин — MinIO (Ceph/Rook слишком тяжёл для одной машины); **заменён на Garage, см. D4a**.
- Появились требования, которых раньше не было: Consul как DCS для Patroni и Infra LB как отдельный уровень (бэкапы и Prometheus есть на схеме, но в лабораторию не входят — D8).

**Ограничение соответствия:** на диаграмме «Redis Cluster», а chart 1.19.2 принимает только `redis` и `redis+sentinel` (режима Redis Cluster в values нет). Фактический режим в проде неизвестен — принят Sentinel как предположение (D3).

## Базовая линия (унаследована, проверена на harbor-on-kind)

| Компонент | Версия |
|-----------|--------|
| Kind CLI | `v0.30.0` |
| Node image | `kindest/node:v1.34.0@sha256:7416a61b42b1662ca6ca89f02028ac133a309a2a30ba309614e8ec94d976dc5a` |
| MetalLB chart | `0.16.1` |
| ingress-nginx chart | `4.15.1` (app `1.15.1`) |
| Harbor chart / app | `1.19.2` / `2.15.2` |

Ключи chart `1.19.2`, относящиеся к HA (сверено по `helm show values harbor/harbor --version 1.19.2`): `database.type: external` + `database.external.*`; `redis.type: external` + `redis.external.*` (встроенный Redis в 2.15.2 — Valkey, `goharbor/valkey-photon`); `persistence.imageChartStorage.type: s3` (для MinIO — `disableredirect: true`, для самоподписанного сертификата хранилища — `caBundleSecretName`); `replicas` у `core`, `portal`, `registry`, `jobservice`, `trivy` (по умолчанию везде `1`).

## Версии компонентов HA (зафиксированы 2026-09-24, H0.1)

Проверено по реестрам (Docker Hub / quay.io / PyPI) и по образам Harbor 2.15.2 на 2026-09-24. Устанавливать только эти версии; менять — сначала здесь и в `CLAUDE.md`.

| Компонент | Pin | Обоснование |
|-----------|-----|-------------|
| PostgreSQL | `postgres:18.6-alpine3.24@sha256:77f585114c32fbca283dc835b0596f4e52b51b4c6662d7810b2f4084f60a1873` | Встроенная БД Harbor 2.15.2 (`goharbor/harbor-db`) — PostgreSQL 18.3; берём тот же мажор 18 |
| Patroni | `4.1.5` (PyPI, extras `consul` + `psycopg3`) | Готового поддерживаемого образа нет (Spilo: последний релиз 2023), поэтому свой образ: слой Patroni поверх образа PostgreSQL выше; версия базового Python — при сборке в Phase 2 |
| Consul | `hashicorp/consul:1.22.7@sha256:a230dcea0bb107bd7958a912d1429fb7f9d399637de7ffb814b34412b9e8c543` | Последний 1.x. Есть 2.0.x (2.0.4), но это свежий мажор: совместимость с Patroni не проверена — не берём |
| HAProxy | `haproxy:3.4.4-alpine3.24@sha256:52c5921e1619f39cbd5b25e1b4b5847667917f39745056cf004d9c263fbf11b9` | Текущий LTS (тег `lts` указывает на 3.4) |
| Valkey (+ Sentinel) | `valkey/valkey:9.0.6-alpine3.24@sha256:187679e3bd4036959631e3f03983ab2ba503ab21e6fd0454d508e909db2ee989` | Встроенный Redis Harbor 2.15.2 — Valkey 9.0.3; тот же мажор. Sentinel — `valkey-sentinel` из того же образа (D3) |
| Garage (S3) | `dxflrs/garage:v2.4.1@sha256:9c96caa2612d3411acc5b0e6701fb238dbfba33e533a6d7d3d811a4b12d0d020` | S3-хранилище стенда (замена MinIO, D4a): последний релиз проекта (2026-09-08), образ на Docker Hub без авторизации, один статический бинарь со своим CLI (`/garage`) для начальной настройки; проверено смоук-тестом: PUT/GET/LIST, multipart (60 МБ частями по 8 МБ), range, delete, abort, изоляция бакета по ключу |
| ~~MinIO~~, ~~MinIO client `mc`~~ | `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z`, `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z` | **Сняты с использования 2026-09-24:** оба репозитория на quay.io стали закрытыми (`401 Requires authentication`), на Docker Hub их нет, `minio/mc` на GitHub в архиве; закреплённые образы получить негде (см. D4a) |

**Заметки:**
- S3: MinIO заменён Garage (D4a). Garage — стенд-ин для Ceph RGW (D4), не эталон: у него нет части возможностей MinIO (нет консоли, версионирование и др.), Harbor/registry их не использует.
- PostgreSQL под Patroni: чарта/оператора не берём, манифесты свои (способ развёртывания — Phase 2). Образы закреплены и по тегу, и по digest.
- Совместимость Harbor 2.15.2 с внешним PostgreSQL 18 подтверждена в H3.3 (миграции прошли, 49 таблиц, push/pull работают).

## Решения и открытые вопросы

**Открытых решений нет** (все закрыты 2026-09-24). Если что-то изменится — вернуть сюда.

**Решено / закрыто:**

- **D6 — критерий успеха в два этапа** (2026-09-24, пользователь): *первым* успехом считается работа всего стенда **с учётом распределения по 14 нодам** (веха 1, ниже); *только после неё* засчитываются остальные проверки (Phase 4, веха 2). Проверки Phase 4 до успеха вехи 1 успехом не считаются.
- **D1 — 14 нод** (2026-09-24): 1 control-plane + 13 воркеров, роль узла — label/taint: `app`×2, `lb`×2, `pg`×2, `redis`×3, `consul`×3, `s3`×1. Consul и MinIO живут на отдельных нодах, Consul — 3 сервера. *Предположение:* MinIO — одна нода (число не называлось). Имена нод в лаборатории — по аналогии с `hb-*` из прод-схемы. Infra LB — ingress-nginx на имеющихся нодах.
- **D2 — Patroni + Consul** (подтверждено пользователем 2026-09-24): PostgreSQL на 2 узла, состояние кластера в Consul. Версии и способ развёртывания — в Phase 2.
- **D3 — Redis: Sentinel (1 master + 2 replica) — ПРЕДПОЛОЖЕНИЕ.** Боевой режим пользователю неизвестен; на схеме «Redis Cluster», а chart 1.19.2 его не поддерживает (только `redis` и `redis+sentinel`). Пересмотреть, если станет известен реальный режим.
- **D4 — блобы registry в S3** (закрыто схемой: Ceph RGW №14; подтверждено: в лаборатории MinIO вместо Ceph).
- **D4a — S3-стенд-ин: Garage вместо MinIO** (2026-09-24, решение пользователя: «другая S3-реализация»; конкретный проект выбран мной из RustFS/Garage/SeaweedFS). Причина: при пересборке с нуля (H5.3) `quay.io/minio/minio` и `quay.io/minio/mc` оказались закрытыми (401), на Docker Hub их нет, закреплённый образ получить негде. Выбран **Garage v2.4.1** (сообщество Deuxfleurs, релиз 2026-09-08, Docker Hub): зрелее RustFS (1.0.0 вышел 2026-09-16), проще SeaweedFS для одной ноды, свой CLI внутри образа. Роль ноды `s3` и бакет `registry-blobs` сохраняются; Service переименован `minio` → `s3` (`s3.harbor-deps:3900`), Secret `minio-credentials` → `s3-credentials`, `make minio` → `make s3`. Из-за смены хранилища milestone 1 и Phase 4 переподтверждаются на новом стенде (H5.3).
- **D5 — один лабораторный кластер за раз** (2026-09-24): `CLUSTER=harbor`, `LB_IP=172.20.0.100` и пул `172.20.0.100–110` остаются как в `harbor-on-kind`. Перед `make cluster` здесь — `make cluster-delete` в старом репо (записи в `/etc/hosts` и `insecure-registries` переиспользуются).
- **D7 — Harbor LB = HAProxy ×2** на `lb`-нодах (подтверждено пользователем 2026-09-24). PG: health-check через Patroni REST API (эндпоинт роли primary — сверить по документации выбранной версии Patroni). Redis: проверка роли master. Harbor обращается к PG и Redis по одному адресу Service перед двумя HAProxy; при таком фронте Harbor получает обычный `redis`-адрес (режим `redis+sentinel` остаётся запасным вариантом). **Отклонение от прода:** там общий адрес, вероятно, держит keepalived, в лаборатории — ClusterIP Service. Infra LB — ingress-nginx + MetalLB.
- **D8 — минимальный объём** (2026-09-24): воспроизводим Harbor app ×2, Harbor LB ×2, PostgreSQL ×2 + Consul (Patroni), Redis ×3, S3 для блобов (MinIO), Infra LB. **Бэкапы PG/Redis и Prometheus с диаграммы в лабораторию не входят.**
- **D9 — цвета green/yellow:** для лаборатории не важны; сетевую сегментацию не делаем.
- **D10 — Nexus не входит** (не запрошен; находится вне блока Harbor на диаграмме).

## Критерии успеха (D6)

**Веха 1 — «стенд работает в распределении по 14 нодам».** Принимается, только когда выполнено всё:

1. 14 нод `Ready`; на воркерах роли по label/taint: `app`×2, `lb`×2, `pg`×2, `redis`×3, `consul`×3, `s3`×1.
2. Каждый компонент запущен на ноде *своей* роли (`kubectl get pods -A -o wide`): нет подов Harbor/PG/Redis/Consul/HAProxy/S3 на чужих нодах; реплики одной роли лежат на *разных* нодах.
3. Кластерные сервисы здоровы: Consul — 3 сервера и есть лидер; Patroni — 1 leader + 1 replica (репликация идёт); Redis — 1 master + 2 replica и кворум Sentinel; HAProxy ×2 оба `Ready`; Harbor core / portal / registry / jobservice — по 2 реплики на двух `app`-нодах; бакет MinIO доступен.
4. Сквозной путь через всю цепочку (Infra LB → Harbor app → Harbor LB → PG/Redis, блобы в S3: MinIO, с D4a — Garage): UI отвечает `200`; push/pull образа и OCI-чарта проходят; `make deploy-app` проходит; блобы лежат в бакете MinIO, а не в томе registry.
5. Нагрузка действительно распределяется: серия pull/запросов через Infra LB обслуживается **обеими** `app`-репликами; HAProxy отдаёт трафик на текущий primary PG.

**Веха 2 — проверки отказоустойчивости** (Phase 4, H4.1–H4.7): выполняются и засчитываются только после успеха вехи 1.

## Последовательный план

Выполнять по фазам. Любой новый компонент (PostgreSQL, Redis, MinIO, операторы) — только с явно зафиксированной pinned-версией здесь и в `CLAUDE.md` до установки.

### Phase 0 — Решения и подготовка

- [x] **H0.1** D6 закрыт, pinned-версии Patroni/PostgreSQL, Consul, HAProxy, Valkey/Sentinel, MinIO зафиксированы (2026-09-24) — раздел «Версии компонентов HA» выше и таблица в `CLAUDE.md`.
- [x] **H0.2** `CLUSTER` / `LB_IP` / MetalLB-пул оставлены как в `harbor-on-kind` (D5). Если HA потребует больше LB-IP — расширить пул в той же подсети и обновить связанные файлы.
- [x] **H0.3** Что шарить между репликами Harbor (2026-09-24; сверено по `helm show values` и шаблонам chart 1.19.2, документации Harbor HA):
  - **Блобы registry:** `persistence.imageChartStorage.type: s3` (+ `disableredirect: true` для MinIO). При `type != filesystem` chart не создаёт PVC registry, а `registry-data` — `emptyDir`; общий том не нужен.
  - **Логи jobservice:** по умолчанию `jobLoggers: [file]` — один RWO-PVC на все реплики, на двух нодах не работает. Ставим `jobLoggers: [database]` (так советует и документация Harbor HA для варианта без RWX); тогда PVC jobservice не создаётся (создаётся только при `file`). RWX StorageClass в KinD не нужен.
  - **Trivy:** StatefulSet со своим `volumeClaimTemplates` (RWO на реплику); общий том не нужен, реплики независимы, очередь сканирования и результаты — в Redis (`SCANNER_STORE_REDIS_URL`). Каждая реплика скачивает свою базу уязвимостей. Решение о числе реплик — H3.2.
  - **Секреты, которые обязаны совпадать у всех реплик и переживать `helm upgrade`:** задавать явно, не полагаться на случайную генерацию: `secretKey` (16 символов, шифрует данные в БД; дефолт `not-a-secure-key` не годится), `core.secret`, `core.xsrfKey`, `jobservice.secret`, `registry.secret`, и **`core.tokenKey` / `core.tokenCert`** (иначе chart на каждом `helm upgrade` генерирует новый token-CA через `genCA`, и во время rolling update реплики core и registry могут подписывать/проверять токены разными ключами). Остальные секреты chart сохраняет через `lookup`, но для воспроизводимого стенда лучше положить всё в `existingSecret`. Пароли лаборатории (`harborAdminPassword`, БД) — как в базовой линии, только для стенда.
  - **Не шарится:** `core` — `emptyDir` (`psc`, только для кэша), `portal` stateless; PVC redis/database chart не создаёт при `external`.
  - **Влияние на план:** H3.1 — `jobLoggers: [database]`, `imageChartStorage.type: s3`, явные секреты выше; H2.5 — MinIO-бакет и ключи доступа до установки Harbor.

### Phase 1 — Кластер

- [x] **H1.0** Предусловия хоста для multi-node KinD (выполнено 2026-09-24): `fs.inotify.max_user_instances=512` и `max_user_watches=524288` (постоянно, в `/etc/sysctl.d/99-kind.conf` — это настройка хоста, в репозитории её нет; на другой машине повторить); свободное место на `/` — 109 ГБ (было 32 ГБ при 97%; каждая нода хранит образы только своей роли — оценка ≈ 5–10 ГБ, не измерялась); RAM: доступно ≈ 31 ГБ из 38 (пользователь подтвердил ~30 ГБ), 16 CPU. Остальные лимиты хоста проверены и достаточны: `fs.file-max`, `kernel.pid_max`, nofile Docker (524288), `TasksMax`, conntrack (262144).
- [x] **H1.0a** Лимиты inotify подняты с запасом под 14 нод (выполнено пользователем 2026-09-24): `fs.inotify.max_user_instances=2048`, `fs.inotify.max_user_watches=1048576` (было 512 / 524288) — постоянно, в `/etc/sysctl.d/99-kind.conf`; значения проверены в живой системе. Это настройка хоста, в репозитории её нет.
- [x] **H1.1** kind-конфиг на 14 нод (D1) — `hack/config/kind-cluster.yaml`, подключён в `make cluster` (2026-09-24). Роль воркера — label `harbor-ha/role=<role>` и taint `harbor-ha/role=<role>:NoSchedule` через `kubeadmConfigPatches` (JoinConfiguration). Имена нод kind: `harbor-worker` (app), `-worker2` (app), `3–4` (lb), `5–6` (pg), `7–9` (redis), `10–12` (consul), `13` (s3). Проверено: создание ≈ 30 с, все 14 нод `Ready`, labels и taints на месте, kindnet/kube-proxy на всех нодах, coredns и local-path-provisioner на control-plane. Замер RAM сразу после создания (`docker stats`): ≈ 2,1 ГБ на 14 контейнеров (control-plane ≈ 576 МБ, воркеры ≈ 115–120 МБ), хост: 29 ГБ доступно; ошибок «too many open files» нет; подсеть `kind` — `172.20.0.0/16`, как в Makefile. Потребление вырастет при установке компонентов — перемерить после Phase 2/3.
  - Из-за taint ingress-nginx и MetalLB speaker сами не запустятся ни на одной воркер-ноде — им нужны tolerations и nodeSelector (H1.2).
- [x] **H1.2** Infra LB на multi-node (2026-09-24): `make infra-lb` (`hack/install-infra.sh`, его же вызывает `hack/install.sh`); values — `hack/config/metallb.yaml`, `hack/config/nginx.yaml`. MetalLB (controller, speaker ×2, frr-k8s) и ingress-nginx ×2 (`podAntiAffinity` по hostname, admission-patch jobs тоже) закреплены на `lb`-нодах через nodeSelector + toleration. Проверено: реплики ingress-nginx на `harbor-worker3` и `harbor-worker4`, Service `172.20.0.100`, IPAddressPool/L2Advertisement на месте. **Решение реализации (не из D1):** Infra LB делит `lb`-ноды с HAProxy (Harbor LB) — отдельных нод под ingress в схеме 14 нод нет; при необходимости вынести — вернуться к D1. Установка пинов та же (MetalLB 0.16.1, ingress-nginx 4.15.1); таймаут ожидания MetalLB поднят с 90 до 180 с (первый старт на 14 нодах медленнее: образы тянутся на каждую ноду).
- [x] **H1.3** LB IP достижим с хоста (2026-09-24): `docker network inspect kind` → `172.20.0.0/16` (совпадает с Makefile и пулом); `curl http://172.20.0.100/` и `https://172.20.0.100/` (-k) → `404` от ingress-nginx (правил Ingress ещё нет — ответ default backend, цепочка MetalLB → ingress работает). ICMP не отвечает (MetalLB L2 не отвечает на ping) — это норма.

### Phase 2 — Общие зависимости

- [x] **H2.1** Consul ×3 (2026-09-24): `make consul` → `hack/ha/00-namespace.yaml` (namespace `harbor-deps` — сюда же пойдут PG/Redis/HAProxy/MinIO) + `hack/ha/consul.yaml` (StatefulSet, образ `hashicorp/consul:1.22.7@sha256:a230…`, свои манифесты без чарта). Реплики на `harbor-worker10/11/12` (все `consul`-ноды, по одной, `podAntiAffinity` + taint toleration), PVC 1 ГБ (`standard`, local-path), PDB `minAvailable: 2`. Клиентский адрес для Patroni — Service `consul.harbor-deps:8500`; headless `consul-headless` для gossip/raft. Проверено: `consul members` — 3 сервера `alive`; `consul operator raft list-peers` — 1 leader + 2 follower (voter), commit index совпадает; из пода в другом namespace (роль `app`) `/v1/status/leader` и `/v1/status/peers` отвечают. Без ACL и TLS (лабораторный стенд; вне D8). Урок: `$(HOSTNAME)` в `args` Kubernetes не подставляет (не входит в env спецификации) — все три сервера получили одно имя и не собрались; имя ноды берётся из `metadata.name` через `POD_NAME`.
- [x] **H2.2** PostgreSQL ×2 под Patroni + Consul (2026-09-24): `make postgres` = `make pg-image` (сборка `hack/ha/patroni/Dockerfile`: `postgres:18.6-alpine3.24@sha256:77f5…` + Patroni 4.1.5 в venv, зависимости зафиксированы в `constraints.txt`; образ `harbor-ha/patroni:4.1.5-pg18.6` загружается `kind load` только в `pg`-ноды, `imagePullPolicy: Never`) + `hack/ha/postgres.yaml` (StatefulSet ×2, `podAntiAffinity`, PVC 5 ГБ, PDB `minAvailable: 1`, headless Service `pg-headless`, конфиг Patroni в ConfigMap; DCS — `consul.harbor-deps:8500`, scope `harbor-pg`). Пароли (`superuser`, `replication`, `harbor`) — Secret `pg-credentials`, генерируется `openssl rand` при первом `make postgres`, в git не попадает. `post_bootstrap` создаёт роль `harbor` и БД `registry` (владелец `harbor`). Проверено: поды на `harbor-worker6` (pg-0) и `harbor-worker5` (pg-1); `patronictl list` — pg-0 Leader/running, pg-1 Replica/streaming, lag 0; REST: `/primary` → 200 на лидере и 503 на реплике, `/replica` → 200 на реплике (это health-check для HAProxy, H2.4); ключи `service/harbor-pg/*` в Consul; под ролью `harbor` на primary работают `CREATE EXTENSION "uuid-ossp"` и запись, строка видна на реплике (`pg_is_in_recovery = t`). Репликация асинхронная (`synchronous_mode` не включён — вне D8). **Ограничения:** образ живёт в containerd нод и пропадает при пересоздании кластера (`make postgres` пересобирает и загружает заново); `pg_hba` пускает по паролю (scram) только из pod-сети `10.244.0.0/16`. Совместимость Harbor 2.15.2 с внешней PostgreSQL 18 — проверить в H3.3 (миграции).
- [x] **H2.3** Valkey ×3 + Sentinel (D3, предположение) (2026-09-24): `make redis` → `hack/ha/redis.yaml` (StatefulSet ×3 на `redis`-нодах, в каждом поде два контейнера: `valkey` и `sentinel` — sidecar, решение пользователя; образ `valkey/valkey:9.0.6-alpine3.24@sha256:1876…`, `podAntiAffinity`, PVC 1 ГБ, `appendonly yes`, PDB `minAvailable: 2`, headless Service `redis-headless`). Пароль — Secret `redis-credentials` (`openssl rand` при первом запуске, в git не попадает); `requirepass` + `masterauth` + `auth-pass` в Sentinel. Сами Sentinel без пароля (доступны только из pod-сети). Топология через стабильные DNS-имена подов (`resolve-hostnames`/`announce-hostnames`, `replica-announce-ip`), а не через pod IP; старт-скрипты в ConfigMap `redis-scripts`: при старте спрашивают Sentinel о текущем master, иначе `redis-0` — master, остальные — реплики `redis-0`; `sentinel.conf` хранится на PVC. Проверено: поды на `harbor-worker7/8/9`; `redis-0` role:master с 2 подключёнными репликами, `redis-1/2` role:slave, `master_link_status:up`; `sentinel master mymaster` — flags master, num-slaves 2, num-other-sentinels 2, quorum 2; `sentinel ckquorum` — «3 usable Sentinels»; без пароля `NOAUTH`; запись на master читается с реплики. **Не проверено (Phase 4 / H4.7):** фактический failover при потере master и возврат старого master как реплики; в H2.4 HAProxy для Redis определяет master по `ROLE`/`INFO replication` с `AUTH`.
- [x] **H2.4** Harbor LB — HAProxy ×2 (D7) (2026-09-24): `make harbor-lb` → `hack/ha/haproxy.yaml` (Deployment ×2 на `lb`-нодах, `podAntiAffinity`, PDB `minAvailable: 1`; образ `haproxy:3.4.4-alpine3.24@sha256:52c5…`; конфиг в ConfigMap `haproxy-config`, DNS бэкендов — через `resolvers` из `resolv.conf`, `init-addr none`). Единый адрес для Harbor: Service `harbor-lb.harbor-deps` — `:5432` (PG) и `:6379` (Redis). PG: HTTP-check `GET /primary` на Patroni REST `:8008` (200 только у лидера), `on-marked-down shutdown-sessions`. Redis: tcp-check `AUTH` (пароль из Secret `redis-credentials` через env) → `PING` → `INFO replication` → ждёт `role:master`. Проверено: поды на `harbor-worker3` и `harbor-worker4` (обе `lb`-ноды); в `/stats` у обоих HAProxy `postgres`: pg-0 UP / pg-1 DOWN, `redis`: redis-0 UP / redis-1,2 DOWN; через `harbor-lb.harbor-deps:5432` запрос попадает на primary (`pg_is_in_recovery = f`, IP pg-0) даже при вызове из пода-реплики; через `:6379` — `set`/`role:master`/`del`; из namespace `default` на `app`-ноде `PING` → `PONG`. Web-статистика — `harbor-lb:8404/stats`. Не проверено (Phase 4 / H4.7): переключение при отказе primary PG / master Redis / одного HAProxy. Замечание: Service — обычный ClusterIP (в проде общий адрес, вероятно, keepalived — отклонение из D7).
- [x] **H2.5** MinIO на `s3`-ноде + бакет (2026-09-24; **заменено Garage — D4a, 2026-09-24**: образы MinIO на quay.io закрылись, реализация ниже — историческая; актуальное — `hack/ha/s3.yaml`, `hack/ha/s3-init.sh`, `make s3`, Service `s3.harbor-deps:3900`, Secret `s3-credentials`): `make minio` → `hack/ha/minio.yaml` (StatefulSet ×1 на `harbor-worker13`, образ `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z@sha256:14ce…`, PVC 10 ГБ, Service `minio.harbor-deps:9000`, консоль `:9001`; Job `minio-init` на образе `mc` `RELEASE.2025-08-13T08-35-41Z@sha256:a7fe…` — новый компонент, версия записана в таблицу выше до установки). Job создаёт бакет **`registry-blobs`**, политику `registry-blobs-rw` (только этот бакет) и пользователя `harbor` с ней; идемпотентен (повторный `make minio` проходит, Job пересоздаётся). Учётные данные — Secret `minio-credentials` (`root-user`/`root-password`, `harbor-access-key`/`harbor-secret-key`; пароли `openssl rand`, в git не попадают). Проверено: под пользователем `harbor` из пода на `app`-ноде `mc cp`/`cat`/`rm` в `registry-blobs` проходят, создание другого бакета — `Access Denied`. **Для H3.1:** `persistence.imageChartStorage.s3`: `regionendpoint: http://minio.harbor-deps:9000`, `bucket: registry-blobs`, `region: us-east-1`, `secure: false`, `disableredirect: true`; chart ждёт `existingSecret` с ключами `REGISTRY_STORAGE_S3_ACCESSKEY` / `REGISTRY_STORAGE_S3_SECRETKEY` — имена ключей в `minio-credentials` другие, поэтому в H3.1 создать отдельный Secret для Harbor из `harbor-access-key`/`harbor-secret-key` (Secret должен лежать в namespace Harbor, не `harbor-deps`). Одна нода, один диск, без erasure coding — отказоустойчивости у хранилища нет (осознанно: D1/D8).
- [x] **H2.6** Доступность из namespace Harbor (2026-09-24; Harbor ставится в `default`, как в baseline): тестовые поды в `default` на `app`-ноде (с toleration роли `app`), образы — те же pinned. PostgreSQL: вход под `harbor` в БД `registry` через `harbor-lb.harbor-deps:5432` → primary (`not pg_is_in_recovery = true`); Consul: `consul.harbor-deps:8500/v1/status/leader` → лидер; Redis: `set`/`get`/`del` с паролем через `harbor-lb.harbor-deps:6379`; MinIO: `mc ls` бакета `registry-blobs` под пользователем `harbor` через `minio.harbor-deps:9000` → ok. Адреса для values Harbor: БД `harbor-lb.harbor-deps:5432` (БД `registry`, пользователь `harbor`), Redis `harbor-lb.harbor-deps:6379` (пароль из `redis-credentials`), S3 `http://minio.harbor-deps:9000`. Секреты живут в `harbor-deps`, а Harbor читает их из своего namespace — в H3.1 скопировать/создать нужные Secret в `default` (пароль БД `harbor`, пароль Redis, ключи S3), не хардкодить в values. **Phase 2 закрыта.**

### Аудит перед Phase 3 (2026-09-24)

- Полная пересборка с нуля (`cluster-delete` → `cluster` → `infra-lb` → `ha-deps`) прошла без ручных шагов: ≈ 7 мин (`cluster` + `infra-lb`) и ≈ 4 мин (`ha-deps`), время в основном — загрузка образов. Добавлен таргет `make ha-deps` (порядок: consul → postgres → redis → harbor-lb → minio).
- После пересборки: 14/14 `Ready`; нет подов не в `Running`/`Completed` и нет рестартов; каждый под на ноде своей роли, реплики на разных нодах; Consul — 1 leader + 2 follower; Patroni — Leader + Replica (streaming, lag 0); Sentinel — «3 usable Sentinels»; оба HAProxy — PG и Redis по одному UP (primary/master); `minio-init` — `init done`; `http://172.20.0.100/` → 404 (ingress жив), подсеть `kind` `172.20.0.0/16`.
- Все манифесты `hack/ha/*.yaml` и `lb-ipaddresspool.yaml` проходят `kubectl apply --dry-run=server`; `yamllint` чист; `bash -n` для скриптов ок; секретов и паролей в репозитории нет (пароли генерируются в Secret).
- Замер памяти (`docker stats`) с Phase 0–2 без Harbor: ≈ 3,9 ГиБ на 14 нод (control-plane ≈ 740 МиБ, `lb`-ноды ≈ 500–540 МиБ, `pg` ≈ 290–310, `s3` ≈ 360, остальные ≈ 120–180); на хосте свободно ≈ 27 ГБ, диск — 97 ГБ. Оценка «≈ 12–13 ГиБ» относилась ко всему стенду с Harbor — перемерить после H3.3.
- Найдено и исправлено при подготовке ранбука: под `metallb-frr-k8s-statuscleaner` не имел закрепления за `lb` (subchart frr-k8s игнорирует nodeSelector для него) — в `hack/config/metallb.yaml` добавлен `nodeAffinity` (`frr-k8s.frrk8s.affinity`), применено `make infra-lb` (helm revision 2), под перезапущен на `lb`-ноде, ingress и MetalLB работают (`http://172.20.0.100/` → 404).
- Замеченное и не ошибка: при холодном старте образов `ErrImagePull`/`ImagePullBackOff` (сбой токена Docker Hub, разовый `NotFound` на quay.io) — Kubernetes ретраит, поды поднимаются сами.
- Известные ограничения перед Phase 3: baseline-`make install` / `make deploy-app` на этом кластере не работают (нет tolerations в `harbor.yaml`) — чинится в H3.1; правка конфига HAProxy требует ручного bump аннотации `config-version`; failover PG/Redis/HAProxy не проверялся (веха 2).

### Phase 3 — Harbor в HA

- [x] **H3.1** `hack/config/harbor-ha.yaml` + `hack/install-harbor-ha.sh` (`make harbor-ha`, `DRY_RUN=1` — только рендер по кластеру) (2026-09-24). Ключи chart 1.19.2 сверены по `helm show values`/шаблонам. Содержимое: внешняя БД `harbor-lb.harbor-deps…:5432` (БД `registry`, пользователь `harbor`), внешний Redis `harbor-lb.harbor-deps…:6379` (режим `redis`, не sentinel), `imageChartStorage.type: s3` (MinIO, `disableredirect: true`, `regionendpoint http://minio.harbor-deps…:9000`, `secure: false`, бакет `registry-blobs`), `jobservice.jobLoggers: [database]`, `replicas: 2` у core/portal/registry/jobservice + `nodeSelector app` + toleration + `topologySpreadConstraints` по hostname (`maxSkew: 1`, `matchLabelKeys: [pod-template-hash]`; изначально был `podAntiAffinity`, заменён в H3.3 из-за блокировки rolling update, а `matchLabelKeys` добавлен в H3.5, иначе после rollout обе реплики могли оказаться на одной ноде) + PDB `minAvailable: 1`; Trivy — `replicas: 1` на `app`-ноде (решение H3.2); ingress `className: nginx`, TLS `auto`. **Секреты** (скрипт создаёт один раз в `default`, никогда не пересоздаёт): `harbor-ha-secrets` (пароль БД, пароль Redis, `secret`, `CSRF_KEY`, `JOBSERVICE_SECRET`, `REGISTRY_HTTP_SECRET`, `secretKey` — случайные, фиксированные между `helm upgrade`), `harbor-ha-s3` (только S3-ключи: chart подмешивает этот Secret в registry целиком через `envFrom`, поэтому отдельный), `harbor-ha-token` (TLS-пара для подписи токенов, общая для всех реплик core; иначе chart на каждом upgrade генерирует новую). **Найдено при проверке:** (1) chart читает `existingSecret` для Redis/БД через `lookup` при рендере — Secret должен существовать до `helm install`, а `helm template` без кластера даёт пустой пароль в `_REDIS_URL_*`; проверка — `helm upgrade -i --dry-run=server` (пароль подставляется во всех трёх местах: core, registry, trivy). (2) `database.maxOpenConns` по умолчанию 900 на pod при `max_connections=200` у PG — выставлены `maxIdleConns: 20`, `maxOpenConns: 50` (2 core ≈ 100 < 200). (3) chart-овский `nginx` при `expose.type: ingress` не разворачивается. **Проверено (без установки):** рендер даёт 4 Deployment (core/jobservice/portal/registry по 2 реплики, все на `app`, с anti-affinity) + StatefulSet trivy ×1 на `app`, нет внутренних `harbor-database`/`harbor-redis`, dry-run на сервере проходит (28 объектов), Harbor-релиза в кластере нет. Реальная установка и `Ready` — H3.3.
- [x] **H3.2** Trivy: одна реплика (решение пользователя, 2026-09-24). Обоснование: StatefulSet со своим RWO-томом на реплику, общий том не нужен, реплики независимы (очередь и результаты — в Redis), но каждая реплика отдельно качает базу уязвимостей; Trivy не входит в критерии вехи 1 (D6). Число реплик — `trivy.replicas` в `hack/config/harbor-ha.yaml`; вернуться, если понадобится проверять отказ сканера (веха 2).
- [x] **H3.3** Установка Harbor 1.19.2 (app 2.15.2) в HA (2026-09-24): `make harbor-ha` (и `make install` = `install-infra.sh` + `install-harbor-ha.sh`). Итог: core/portal/registry/jobservice по 2 реплики `Running`, по одной на `harbor-worker` и `harbor-worker2` (обе `app`), trivy ×1 на `app`, внутренних `harbor-database`/`harbor-redis` нет; UI `https://core.harbor.domain/` → 200. Harbor работает на внешних сервисах: миграции прошли на PostgreSQL 18 (49 таблиц в `registry`, проект `library`, 2 пользователя; реплика PG их видит, `pg_is_in_recovery = t`), в Redis на master заняты db0 (68 ключей) и db1 (28). **Найдено и исправлено при установке:** (1) **ключ токена в формате PKCS#8** — `openssl req -newkey` даёт `BEGIN PRIVATE KEY`, core отвечает 500 на `/v2/` (`unable to get PrivateKey from PEM type: PRIVATE KEY`); теперь `openssl genrsa -traditional` (PKCS#1, `BEGIN RSA PRIVATE KEY`), Secret `harbor-ha-token` пересоздан. Если у кого-то Secret создан старой версией скрипта — удалить его и запустить `make harbor-ha`. (2) **Взаимная блокировка rolling update при обязательном `podAntiAffinity` на 2 нодах**: surge-поду некуда встать (нужна 3-я нода), а `maxUnavailable` от 25% при 2 репликах округляется до 0; ещё и старые поды несут прежнее правило (сообщение «didn't satisfy existing pods anti-affinity rules»). Исправление: для core/portal/registry/jobservice — `topologySpreadConstraints` (`maxSkew: 1`, `DoNotSchedule`) вместо anti-affinity; для `harbor-lb` (2 `lb`-ноды, anti-affinity сохранён) — `strategy: RollingUpdate, maxSurge: 0, maxUnavailable: 1`. При переходе пришлось один раз пересоздать Deployment'ы (`scale 0` → `2`); проверено, что после этого `rollout restart` core и registry и bump `config-version` у `harbor-lb` проходят. (3) jobservice при первом старте делает 2–3 рестарта (core ещё не принимает соединения: `connection refused` на `harbor-core:80/api/v2.0/internalconfig`) и затем работает — штатная гонка запуска, не ошибка.
- [x] **H3.4** Доверие CA, `make deploy-app` (2026-09-24): хост уже доверял `core.harbor.domain` (`insecure-registries`, `/etc/hosts`), `make deploy-app` проходит за ≈ 15 с: проект `python`, `docker login`/build/push `core.harbor.domain/python/hello:1.0` (digest `sha256:4a841ff3…`), CA Harbor в containerd control-plane-ноды, pull secret, деплой. **Решение реализации (новое, не из D1):** demo-приложение запускается на **control-plane** — воркеры под таинтами ролей, а `deploy-app.sh` ставит CA только на control-plane; поэтому в `python-docker-hello-kube/deployment.yml` и `helm-hello-kube` (values `nodeSelector`/`tolerations`, шаблоны deployment и test-pod) добавлены selector и toleration `node-role.kubernetes.io/control-plane`. Установка CA на все ноды не делалась. Проверено: `hello-service` `172.20.0.101:5000` отвечает от разных подов; OCI-чарт: `helm registry login` → `helm package` → `helm push oci://core.harbor.domain/python/hello` → `helm pull` (тот же digest `sha256:3cb1f37d…`) → `helm install` из OCI → `helm test` (Phase: Succeeded) → `helm uninstall`. **Блобы в MinIO, а не в томе:** `mc ls -r registry-blobs` — 16 объектов, 17 МиБ (среди них блоб с digest манифеста образа `4a841ff3…`); PVC registry нет (единственный PVC Harbor — `data-harbor-trivy-0`).
- [x] **H3.5** Приёмка вехи 1 (2026-09-24): все пять пунктов «Критериев успеха (D6)» выполнены; результат ниже. **Принята пользователем 2026-09-24** — с этого момента проверки Phase 4 (веха 2) засчитываются.
  1. **14 нод, роли.** 14/14 `Ready`; воркеры: `app` 2, `lb` 2, `pg` 2, `redis` 3, `consul` 3, `s3` 1 (+ control-plane без роли), на всех 13 воркерах таинт роли.
  2. **Размещение.** Проверка V2.4 из `docs/verification-runbook.md` — пусто (нет подов не на своей роли; ingress-nginx и MetalLB — на `lb`); повторов «одна роль — одна нода» нет: consul-0/1/2 на трёх `consul`-нодах, pg-0/1 на двух `pg`, redis-0/1/2 на трёх `redis`, harbor-lb ×2 и ingress-nginx ×2 на двух `lb`, minio-0 на `s3`; core/portal/registry/jobservice — по одной реплике на `harbor-worker` и `harbor-worker2`. **Найдено и исправлено при приёмке:** после `rollout restart` обе реплики core оказались на одной ноде — `topologySpreadConstraints` не перераспределяет запущенные поды, а старая и новая ревизии считались вместе. Исправление: `matchLabelKeys: [pod-template-hash]` во всех четырёх компонентах Harbor (revision 4 релиза); проверено — 5 подряд `rollout restart` всех четырёх Deployment'ов, после каждого ровно 1+1 на двух `app`-нодах.
  3. **Кластерные сервисы.** Consul: consul-0 leader, consul-1/2 follower; Patroni: pg-0 Leader/running, pg-1 Replica/streaming; Redis: master + 2 slave, Sentinel `ckquorum` — «3 usable Sentinels»; HAProxy ×2 `1/1 Running`, у обоих postgres: pg-0 UP / pg-1 DOWN, redis: redis-0 UP / redis-1,2 DOWN; Harbor: core, portal, registry, jobservice — по `2/2`, trivy `1/1`; MinIO `minio-0` Running, `minio-init` Complete, бакет `registry-blobs` доступен; неисправных подов 0.
  4. **Сквозной путь через Infra LB.** UI `https://core.harbor.domain/` → 200, `/api/v2.0/systeminfo` → 200, `/v2/` → 401 (нужен токен); `docker login` успешен; `make deploy-app` проходит (push `hello:1.0`, digest `sha256:4a841ff3…`); `docker rmi` + `docker pull` возвращают тот же digest; OCI-чарт: `helm push` → `helm pull` (digest `sha256:d6bcfdc7…`), ранее также `helm install` из OCI и `helm test` → Succeeded; блобы в MinIO: `mc du registry-blobs` — 26 объектов, 17 МиБ; PVC registry нет (единственный PVC Harbor — `data-harbor-trivy-0`).
  5. **Распределение нагрузки.** 100 запросов к `/api/v2.0/systeminfo`, 100 к `/`, 60 к `/v2/` и 20 `docker pull` через Infra LB; по адресу получателя в логах ingress-nginx: core — 127 и 131 запросов на двух подах, portal — 50 и 50; по логам registry `GET /v2/`: 20 и 18 (обе реплики); оба ingress-nginx обработали трафик (239 и 175 записей). HAProxy держит на primary: у пользователя `harbor` на `pg-0` 9 соединений (от 2 адресов — обоих HAProxy), на `pg-1` — 0.
  Ограничение приёмки: failover, потеря реплики/ноды и остальные проверки отказоустойчивости не выполнялись (это веха 2, Phase 4).

### Phase 4 — Проверка отказоустойчивости (веха 2: начинать только после H3.5)

- [x] **H4.1** push/pull образа и OCI-чарта при всех репликах (2026-09-24, веха 2; засчитывается — веха 1 принята). Условия: все реплики работают (core, portal, registry, jobservice — `2/2`, trivy `1/1`, зависимости `Running`). Ход: собраны два образа `hello:h41-a` / `h41-b` с разными слоями по 40 МБ случайных данных (чтобы registry делал multipart-загрузку в S3, chunk по умолчанию 5 МБ), запушены **параллельно** — оба успешно (digest `sha256:9bc1d710…` и `sha256:f90e67ca…`); после `docker rmi` оба скачаны обратно с теми же digest; в API Harbor артефакты есть (размер ≈ 59,7 МБ в сжатом виде); под из `h41-a` (pull делает containerd ноды через pull secret) стал `Ready`, `imageID` совпал с digest; OCI-чарт `hello-kube` 0.1.1: `helm push` → `helm pull` (один и тот же digest `sha256:99d111f3…`) → `helm install` из OCI → `helm test` (Phase: Succeeded) → `helm uninstall`. **Обе реплики участвовали:** registry — PATCH 2 и 2, PUT 5 и 4, GET 12 и 9; core (`/v2/` через ingress, по адресу получателя) — 35 и 31. Блобы в MinIO: 17 МиБ → 97 МиБ (44 объекта). Тестовые артефакты (`h41-a`, `h41-b`, чарт 0.1.1) удалены через API, тестовые поды убраны. Замечание: удаление артефакта в Harbor не освобождает блобы в S3 — место вернёт только сборка мусора (GC) registry; в лаборатории её не запускали. В API репозитория чарта остаётся один безымянный артефакт (перезаписанный при повторном пуше 0.1.0 в H3.5) — не связан с H4.1.
- [x] **H4.2** Удаление пода registry/core *во время* push (2026-09-24, веха 2). Скрипт: `hack/tests/h42-kill-during-push.sh <registry|core> <container> <tag>` (строит образ с несжимаемыми слоями, ограничивает скорость app-нод через `tc`, чтобы загрузка длилась ~2 минуты, по росту `eth0 rx` находит под, который *сейчас принимает* загрузку, и убивает его `--force --grace-period=0`; после push сравнивает digest, чистит за собой по digest). Образ: 2 слоя по 200 МБ.
  - **registry:** убит `harbor-registry-…-sw8rn` (`harbor-worker2`) в 11:06:17 UTC, через ~0,005 с после обнаружения активной загрузки (rx +27 МБ за опрос). `docker push` продолжил и завершился в 11:08:19 с `exit=0` и digest `sha256:85637748…`; клиент сам повторил загрузку слоя (15 строк `Retrying in N s`). ingress-nginx: 2 × `PATCH 502` в момент убийства, затем `PATCH 202` ×3, `PUT 201` ×4. После `docker rmi` образ скачан обратно — digest тот же, оба слоя по 209 715 200 байт. Реплика пересоздана (`…-5tf6r`, `harbor-worker2`), Deployment `2/2`.
  - **core:** убит `harbor-core-…-lc8lj` в 11:15:14 UTC (rx +4 МБ за опрос). `docker push` завершился в 11:17:35, `exit=0`, digest `sha256:a7bca236…`, 5 строк `Retrying`; ingress: `PATCH 502` ×1 (плюс запись `upstream …` о закрытом соединении), затем `PATCH 202` ×3, `PUT 201` ×4. Pull обратно — digest совпал, слои 209 715 200 байт. Реплика пересоздана (`…-kjlp9`, `harbor-worker`).
  - **Вывод:** потеря активной реплики registry или core посреди загрузки не приводит к потере или порче данных: клиент получает 502, повторяет запрос, загрузка завершается на оставшейся реплике. Push «корректно повторяется» (критерий пункта выполнен). Push в Harbor при этом не был прозрачным: клиент видел ошибку и делал повтор (~15 с для слоя), так что «без ошибок у клиента» для H4.2 не заявляется.
  - **Не засчитанные попытки (для честности):** три прогона без убийства (push успевал завершиться до обнаружения; на localhost 1–4 ГБ уходят за секунды; в контейнере registry нет `awk`, детектор сначала читал пустое значение), один прерванный оборвавшейся сессией (3 слоя в ретраях, итог неизвестен), одна попытка на core, испорченная сбоем Redis от перегрузки диска (см. ниже: push отклонён с `unauthorized`, убийства не было); повторена после стабилизации и прошла.
  - **Побочные находки (важные для всей веха 2):** (1) **Перегрузка общего диска хоста дестабилизирует весь стенд.** Гигабайты записи при тестах (сборка образов с `dd`, push по 1–4 ГБ) поднимали load average до 30+. Следствия: `kube-controller-manager` и `kube-scheduler` теряли leader-election (`leaderelection lost`, 7 рестартов, `CrashLoopBackOff`) — Kubernetes на минуты переставал пересоздавать поды (реплика после убийства не воссоздавалась, пока control-plane не вернулся); Valkey писал `Asynchronous AOF fsync is taking too long (disk is busy?)`, Sentinel несколько раз переключал master (redis-0 → redis-1 → redis-2), HAProxy каждый раз сам находил нового master; у core, jobservice (7–9), pg, frr-k8s и apiserver были рестарты по liveness-проб. Меры: **leader-election тайминги** `lease 60s / renew 40s / retry 10s` для controller-manager и scheduler (`hack/config/kind-cluster.yaml`, кластерный `kubeadmConfigPatches`; на живом кластере применено правкой static-pod манифестов — после этого рестартов нет); **`down-after-milliseconds` Sentinel 5000 → 15000** (`hack/ha/redis.yaml` и `SENTINEL SET` на всех трёх Sentinel в живом кластере). Оба изменения — настройка под лабораторию с одним диском, а не архитектурное решение; влияют на H4.7 (Redis-master теперь обнаруживается за ~15 с вместо ~5 с). Изменение в `kind-cluster.yaml` проверено на временном одно-нодовом кластере (тот же кластерный `kubeadmConfigPatches`: kubeadm отрисовал все три флага у controller-manager и scheduler, штатный флаг kind сохранился; кластер удалён, стенд не затрагивался); полная пересборка стенда со всеми исправлениями — в H5.3. (2) **Ошибка очистки:** тег `h42-auth` был поставлен на образ `hello:1.0` (один digest), а удаление артефакта в Harbor удаляет его целиком со всеми тегами — пропал `hello:1.0`; восстановлен `make deploy-app` (тот же digest `sha256:4a841ff3…`). Скрипт теперь удаляет только по digest собственного образа. (3) В MinIO остались 14 нулевых `_uploads/<uuid>/data` (незавершённые multipart); есть и у пушей без убийства (10:43, 11:02 UTC), то есть это штатный мусор S3-драйвера registry, а не следствие убийств; убирается purge-задачей registry/GC. Бакет вырос до 8,5 ГиБ (121 объект) из-за не запускавшегося GC после удаления тестовых артефактов. (4) HAProxy/Patroni/Consul при этом не пострадали.
- [x] **H4.3** Rolling update core и registry во время непрерывных pull (2026-09-24, веха 2). Скрипт: `hack/tests/h43-rolling-update.sh` (+ `h43_analyze.py`): через Infra LB идут три нагрузки — `GET /v2/python/hello/manifests/1.0` каждые ~0,1 с, `GET` самого большого слоя (13 МБ, registry отдаёт из MinIO) каждые ~0,4 с и `docker rmi` + `docker pull python/hello:1.0` подряд; curl **не** повторяет запросы, поэтому каждая ошибка — это ошибка, которую увидел бы клиент; `kubectl rollout restart` core, затем registry, нагрузка идёт до, во время и после. Нагрузка лёгкая (диск общий, см. H4.2).
  - **Первый замер (без исправления), 4 прогона:** core — ошибок нет ни в одном прогоне; registry — в 2 прогонах из 4 по одной ошибке: `502` на запрос манифеста и `docker pull` с `received unexpected HTTP status: 502 Bad Gateway` (всего 1/749 запросов манифеста, 0/340 блоба, 1/350 pull), плюс паузы запросов ≈ 5,2–5,3 с. Причина: при остановке пода registry kube-proxy ещё держит его endpoint, core через Service `harbor-registry` получает `dial tcp 10.96.35.155:5000: connect: connection refused` (`http: proxy error` в логе core) и отвечает клиенту 502; docker-клиент часть таких случаев повторяет сам. В chart есть `terminationGracePeriodSeconds: 120`, но нет `preStop` и нет значений для него.
  - **Исправление:** `hack/helm-postrender.py` — post-renderer Helm (подключён в `hack/install-harbor-ha.sh`, `--post-renderer`), добавляет `lifecycle.preStop: sleep 15` всем контейнерам core, registry (registry, registryctl) и portal: старый под ещё обслуживает запросы, пока маршрутизация убирает его. Требует python3 + PyYAML на хосте. В образах есть `sh` и `sleep` (проверено). Применено `make harbor-ha` (helm revision 5), распределение по нодам после rollout 1+1.
  - **Замер после исправления, 4 прогона:** 0 ошибок — 0/746 запросов манифеста, 0/340 блоба, 0/480 `docker pull`; 4 прогона из 4 чистые; самый медленный запрос 0,5 с (5-секундные паузы исчезли). Суммарно за 8 прогонов: до — 2 ошибки в 2 из 4 прогонов, после — ни одной. **Оговорка:** выборка небольшая (ошибка до исправления возникала примерно в 1 из 350–750 запросов), «0 ошибок» подтверждает улучшение, но не доказывает нулевую вероятность; UI (portal) нагрузкой не покрывался, хотя preStop для него тоже добавлен; jobservice не менялся (клиентский путь pull его не использует).
  - **Итог для критерия H4.3:** после исправления rolling update core и registry проходит без ошибок у клиента. До исправления критерий выполнялся не полностью (редкие 502) — это найденный и устранённый дефект стенда, а не свойство Harbor.
- [x] **H4.4** Потеря worker-ноды (D1 = multi-node; 2026-09-24, веха 2). Скрипт: `hack/tests/h44-node-loss.sh [нода]` (+ `h44_analyze.py`): под той же нагрузкой, что в H4.3 (манифест каждые ~0,1 с, блоб 13 МБ каждые ~0,4 с, `docker pull` подряд, curl без повторов), нода «умирает» через `docker kill` (без корректной остановки), затем возвращается `docker start`. Убивалась `app`-нода: два прогона.
  - **Прогон 1: `harbor-worker2`** (core, portal, registry, jobservice, trivy; ждали вытеснения). Хронология относительно падения: +50 с нода `NotReady` (Kubernetes признаёт потерю только через `node-monitor-grace-period` ≈ 40 с плюс период статуса), в тот же момент из Endpoints убраны её поды; +353 с (≈ 5 мин `tolerationSeconds: 300`) поды вытеснены — замены core/portal/registry/jobservice встали в `Pending`: `0/14 nodes are available: … 1 node(s) didn't match pod topology spread constraints` (ожидаемо: `maxSkew: 1` не даёт поставить вторую реплику на единственную живую `app`-ноду, сервис работает на одной реплике каждого компонента); trivy остаётся `Terminating` на мёртвой ноде (единственная реплика, PVC привязан к ноде) — сканирование недоступно, пока нода не вернётся; +384 с нода запущена, `Ready` через 2 с, все четыре Deployment'а `2/2` через 39 с, trivy `Running` на той же ноде с прежним PVC.
  - **Прогон 2: `harbor-worker`** (без trivy; нода возвращена через ~2 мин, до вытеснения): `NotReady` через 47 с, нода `Ready` через 5 с после старта, Deployment'ы `2/2` через 60 с.
  - **Ошибки клиентов:** прогон 1 — 0 из 1200 запросов манифеста, 0 из 554 блобов, 0 из 561 `docker pull`; прогон 2 — 1 из 466 манифестов (`502` ровно в момент `docker kill`: запрос был «в полёте» на убиваемую ноду, отработает только повтор клиента), 0 из 218 блобов, 0 из 212 pull. **Сервис продолжает отвечать на оставшейся ноде; push/pull проходят** (после возврата ноды дополнительно проверен `docker login` + `docker pull` с тем же digest `4a841ff3…`).
  - **Цена — задержки в первые ~50 секунд**, пока Kubernetes не признал ноду потерянной и не убрал её из Endpoints: до этого ingress-nginx и core (через Service `harbor-registry`) периодически попадают в «чёрную дыру» и ждут таймаут соединения: запросы манифеста до 10,2 с (8 из 12 в этой фазе дольше 2 с), блоба до 6,5 с, `docker pull` до 30,4 с (25,6 с во втором прогоне) — все завершились успешно (ingress повторяет на другую реплику, docker-клиент терпит). После `NotReady` задержки уходят (макс. 1,9–3,1 с, дальше доли секунды). Это свойство настроек Kubernetes (`node-monitor-grace-period`, таймауты соединений), а не Harbor; меры (уменьшить grace-period kube-controller-manager, `proxy-connect-timeout` ingress) не применялись, чтобы не менять стенд без нужды.
  - **Вывод:** потеря одной `app`-ноды не приводит к отказу сервиса и потере данных; клиенты видят до ~50 секунд замедлений, а при потерянных «в полёте» запросах — единичные 502. Ёмкость на время аварии — одна реплика каждого компонента; после возврата ноды всё восстанавливается автоматически до 2/2 и 1+1. PG, Redis, Consul, HAProxy, MinIO при этом не затрагивались (без рестартов). Не проверялось: постоянная потеря ноды без возврата (замена ноды), потеря ноды других ролей (H4.7).
- [x] **H4.5** demo-app: rollout после push нового тега (2026-09-24, веха 2). Скрипт: `hack/tests/h45-app-rollout.sh` (`DEGRADE=1` — перед rollout убиваются один под registry и один под core): считает, какие поды отвечают до rollout; собирает `python/hello:h45-<epoch>` (hello:1.0 + один малый слой), пушит в Harbor; `kubectl set image` на `hello-deployment` при пробнике `hello-service` (`172.20.0.101:5000`, ~10 запросов в секунду, фиксируются имя пода и код); проверяет `imageID` новых подов, события kubelet и распределение ответов; в конце возвращает `hello:1.0` и удаляет тестовый артефакт **по digest**.
  - **Штатный прогон:** push нового тега прошёл; rollout завершился за 9,2 с; оба новых пода на `imageID` = запушенному digest `sha256:4a8c5da8…`; 171 запрос, 0 ошибок; до rollout отвечают обе реплики (22/18 из 40), во время rollout — три (старые и новая), после — обе новые (23/17 из 40).
  - **Прогон с деградацией Harbor** (force-delete одного registry и одного core, замены ещё в `ContainerCreating` — Harbor на одной реплике каждого): kubelet подтянул образ (`Successfully pulled image … in 499ms` и `195ms`, сообщения kubelet сохранены), rollout за 8,6 с, новые поды на digest `sha256:e14325c0…`, 166 запросов, 0 ошибок, после rollout отвечают обе новые реплики (20/20 из 40). На несколько запросов сразу после rollout отвечает ещё завершающийся старый под — у demo-приложения нет `preStop` (это не Harbor).
  - **Найдено при подготовке (первый прогон был неудачным): новые pull образов на ноде ломались после любого `helm upgrade` Harbor.** Первый rollout завис: новый под — `ErrImagePull`, `Head https://core.harbor.domain/v2/…: tls: failed to verify certificate: x509: certificate signed by unknown authority`. Причина: при `expose.tls.certSource: auto` chart генерирует **новый** самоподписанный CA при каждом рендере (`genCA`, срок 365 дней), а containerd на control-plane доверял CA, установленному `make deploy-app` до моего `make harbor-ha` в H4.3 (ревизия 5); уже работающие поды это не задевало, но любой новый pull (в том числе перезапуск подов с `imagePullPolicy: Always`) падал бы. Сервис при этом не пострадал (старые поды отвечали, 0 ошибок в пробнике). Исправление: `hack/install-harbor-ha.sh` один раз создаёт собственные CA и серверный сертификат для `core.harbor.domain` (10 лет, SAN, `serverAuth`) в Secret `harbor-ha-ingress-tls` (`tls.crt`, `tls.key`, `ca.crt`); values: `expose.tls.certSource: secret` + `secretName`, `caSecretName` (тот же Secret, откуда `/api/v2.0/systeminfo/getcert` берёт CA). Проверено: ingress отдаёт этот сертификат (`openssl verify` — ok), `getcert` возвращает тот же CA, один раз выполнен `make deploy-app` для доверия на ноде; **повторный `helm upgrade` (ревизия 7) CA не меняет** (серийник CA `1125BC…` до и после), `crictl pull --creds …` с ноды проходит. Побочно: после этой правки `make deploy-app` больше не обязан пересоздавать доверие после каждого upgrade (шаг остаётся безвредным). **Новое решение реализации (не из D1):** собственный стабильный CA вместо автогенерации chart — остаётся самоподписанным, для лаборатории; в проде здесь нужен настоящий сертификат/cert-manager.
  - **Вывод:** rollout приложения после push нового тега в Harbor проходит, поды подтягивают образ из Harbor (в том числе при деградированном Harbor), запросы к приложению не теряются, нагрузка распределяется по репликам.
- [x] **H4.6** Proxy-cache проект к внешнему registry (Docker Hub) (2026-09-24, веха 2). Настройка через API Harbor: endpoint типа `docker-hub` (`POST /registries`, url `https://hub.docker.com`; Harbor при создании пингует `hub.docker.com`) и проект с `registry_id` (`POST /projects`, `public: true`). Скрипт: `hack/tests/h46-proxy-cache.sh` (13 проверок; проект и endpoint уникальны для каждого прогона и удаляются в конце; `KEEP=1` оставляет). Доступ к Docker Hub с нод и из подов Harbor есть (`registry-1.docker.io/v2/` → 401, `hub.docker.com` → 200); зеркала из `daemon.json` хоста Harbor и нод не касаются.
  - **Работает:** холодный `docker pull core.harbor.domain/<proxy>/library/alpine:3.20` — 4–7 с, digest индекса **совпадает** с digest на самом Docker Hub (`sha256:d9e853e8…`); в проекте появляется репозиторий; проект `python` (локальный) работает рядом, `python/hello:1.0` тянется как раньше; **push в proxy-проект отклоняется** (`denied: can not push artifact to a proxy project`).
  - **Недоступный апстрим (искусственно):** CoreDNS отдаёт NXDOMAIN для `docker.io`/`docker.com` внутри кластера (после смены Corefile CoreDNS перезапускается, исходный Corefile восстанавливается всегда; проверено md5). Под Harbor подтверждённо не достаёт Docker Hub (`curl` → 000). В этих условиях: **кэшированный образ по digest платформы `sha256:c64c687c…` тянется** (и слой скачивается напрямую через Harbor с верным sha256 — сверка независимая от content store docker); это остаётся верным **при убитой любой паре core+registry** (сначала пара №1, затем №2 — выжившие реплики отдают из кэша: данные в MinIO/PostgreSQL общие); **никогда не кэшированный** образ (`hello-world`) тянуться не может (`repository … not found`); после возврата апстрима он тянется, а pull по тегу снова работает.
  - **Ограничения proxy-cache, найденные при проверке (важно для эксплуатации):** (1) **Кэшируется манифест платформы и его блобы по digest, а не тег и не мультиархитектурный индекс.** При недоступном апстриме pull **по тегу не работает** (`artifact …:3.20 not found`: тег → digest резолвится только у апстрима) — proxy-cache даёт устойчивость к недоступности апстрима только для запросов по digest. Pull по digest индекса иногда проходит (под core держит открытое keep-alive соединение с Docker Hub), поэтому в тесте он только сообщается (INFO). (2) **Кэш регистрируется асинхронно** — запись об артефакте появляется в БД через ~17–40 с после первого pull; pull с недоступным апстримом раньше этого срока не из чего обслужить. (3) Для endpoint типа `docker-hub` **поле URL игнорируется**, сломать апстрим изменением URL нельзя (первая попытка теста этим способом дала ложный результат — «кэш» на самом деле шёл в апстрим). (4) **Удаление репозитория/проекта через API не чистит S3**: манифесты и блобы остаются, и повторное проксирование в проект с тем же именем не создаёт запись об артефакте (кэш выглядит пустым, хотя pull работает) — поэтому проект в тесте уникален. (5) Блокировка апстрима через DNS возвращается не сразу (кэш CoreDNS и `reload` — до нескольких минут), поэтому в скрипте CoreDNS перезапускается. (6) Docker Hub ограничивает анонимные pull — в тесте единицы небольших образов; в проде для endpoint нужны учётные данные.
  - **Вывод:** proxy-cache работает на HA-стенде: холодный pull через Harbor с любой реплики, повторные обращения по digest обслуживаются из общего кэша при отказе реплик и при недоступном апстриме; локальные проекты не затронуты. Побочно: `docker pull` по тегу без апстрима — не поддерживается Harbor, это его свойство, а не дефект стенда.
- [x] **H4.7** Отказ по ролям схемы (2026-09-24, веха 2). Скрипт: `hack/tests/h47-role-failure.sh <lb|pg|redis|consul>` (+ `h47_analyze.py`). Нода жертвы убивается `docker kill` (без корректной остановки) и возвращается `docker start`; жертва — держатель роли: для `pg` нода лидера Patroni, `redis` — master Valkey, `consul` — лидер raft, `lb` — нода, которая **анонсирует адрес Infra LB** (владелец ARP `172.20.0.100`: на ней ingress-nginx, HAProxy, активный MetalLB speaker). Под нагрузкой: `curl` манифеста и блоба через Infra LB, `docker pull`, `docker push` каждые ~3 с (пишет в PG, Redis и S3), плюс два «писателя» в подах — `INSERT` в PostgreSQL и `INCR` в Redis через `harbor-lb`. В конце подтверждённые записи сверяются с тем, что осталось в БД (потерянные подтверждённые записи), считаются перезапуски подов. Роль `app` — H4.4 (не повторялась), `s3` в схеме одна нода без резервирования (D1) и не проверялась.
  - **Итоговые результаты (после исправлений ниже), по одному прогону на роль:**

| Отказ | Обнаружен (NotReady) | Восстановление сервиса | Ошибки клиентов | Потери подтверждённых записей | Рестарты подов Harbor |
|---|---|---|---|---|---|
| Consul: нода лидера raft (`consul-1`) | +42 с | новый лидер `consul-2`, без заметного влияния | 0 из 366 манифестов, 168 блобов, 392 pull, 44 push; писатели 0 | 0 (PG 750/750, Redis 692) | нет; Patroni не переключался |
| Redis: нода master (`redis-0`) | +56 с | Sentinel переключил за ≈ 16 с (+`down-after` 15 с), писатель Redis без ответа ≈ 21 с | 0 из 352 / 162 / 374 / 38; зависания запросов Harbor до ≈ 21 с | 0; значения `INCR` не откатывались | нет |
| PostgreSQL: нода лидера (`pg-0`) | +48 с | Patroni повысил `pg-1`, запись возобновилась через ≈ 33 с; старый primary вернулся репликой (`streaming`) | ошибки 5xx у запросов, которым нужна БД, окно ≈ 16 с: манифесты 121 из 446, блобы 37 из 186, push 1 из 31 (`docker pull` — 0 из 516 благодаря повторам клиента); писатель БД без записи ≈ 18 с | 0 (554/554), но репликация асинхронная — гарантии нет | нет |
| lb: нода-анонсёр (`harbor-worker4`: ingress-nginx, HAProxy, MetalLB) | +43 с | адрес Infra LB недоступен ≈ 36 с (переезд L2-анонса MetalLB на другую ноду), ещё сбой ≈ 8 с при возврате ноды; половина новых соединений к БД/Redis через мёртвый HAProxy обрывается ≈ 45 с | манифесты 14 из 306, блобы 10 из 145, push 2 из 34, pull 0 из 330; писатели БД и Redis по 18 ошибок | 0 (PG 523/523; Redis без откатов) | нет |

  Все нагрузки вернулись к норме после возврата ноды: `Consul` 3 сервера (+13 с после Ready ноды), PG — лидер + реплика `streaming` (+24 с), Redis — 1 master + 2 replica, кворум Sentinel (+16 с), lb — ingress-nginx и HAProxy `2/2` (+39 с).
  - **Найдено и исправлено в ходе H4.7 (это главное содержание пункта):**
    1. **Расщепление мозга Redis после возврата ноды (потеря подтверждённых записей).** В первом прогоне `redis-0` после `docker start` запустился **как master** (скрипт старта делал master из `redis-0`, если ни один Sentinel не ответил, а сразу после старта ноды DNS и сеть ещё не готовы); Sentinel конвертировал его в реплику лишь через ≈ 15 с, всё это время HAProxy видел два master и разбрасывал записи на оба: 8 подтверждённых `INCR` ушли на устаревший master (ответы 64, 65, 68… вместо 508…) и пропали при пересинхронизации. Мой первый счётчик потерь этого не видел (сравнивал только максимум) — теперь анализатор считает ответы, которые «идут назад». Исправления: (a) `start-valkey.sh`: при рестарте с данными на томе `redis-0` не становится master сразу, а до ≈ 90 с спрашивает Sentinel и сами серверы Valkey; master без ответа — только при первом запуске или полном холодном рестарте; (b) `--min-replicas-to-write 1 --min-replicas-max-lag 10`: master без реплики отказывает в записи; (c) проверка HAProxy для Redis требует `role:master` **и** подключённую реплику (`role:master[^a-z]{1,4}connected_slaves:[1-9]`).
    2. **Ошибка в моей первой версии проверки HAProxy:** регулярное выражение `role:master.*connected_slaves:[1-9]` не совпадало (в регулярках HAProxy `.` не захватывает перевод строки), все бэкенды Redis стали `DOWN`, и Redis через `harbor-lb` был недоступен несколько минут, пока я не заметил и не исправил (после перезапуска StatefulSet Redis). Harbor это не уронило, но это ошибка, допущенная мной на живом стенде.
    3. **Harbor core и jobservice не переживали переключение Redis: kubelet убивал контейнеры по liveness.** Их пробы (`/api/v2.0/ping`, `/api/v1/stats`) зависают, пока Redis недоступен; значения chart по умолчанию (timeout 1 с, 2–3 неудачи) хватало ~20 с, после чего **оба core перезапускались** (`Container core failed liveness probe, will be restarted`), ingress отвечал 503, пока core не поднимался (≈ 106 из 391 манифестов, 29 блобов, 2 push — ошибки). Исправление в `hack/config/harbor-ha.yaml`: `core.livenessProbe` и `jobservice.livenessProbe` — `timeoutSeconds: 5`, `failureThreshold: 6` (терпит ≈ 60 с; readiness не меняли: под без Redis выпадает из балансировки, но не перезапускается). После этого третий прогон Redis прошёл без ошибок клиентов и без рестартов Harbor.
    4. Тест не удалял за собой таблицу `h47_probe` и ключ `h47:counter` — исправлено (очистка через `harbor-lb`).
  - **Свойства, которые не исправлялись (для эксплуатации):** (1) окна недоступности определяются настройками, а не Harbor: обнаружение потери ноды Kubernetes ≈ 45–55 с, Sentinel ≈ 16 с при `down-after` 15 с, Patroni ≈ 30 с (TTL), MetalLB L2 ≈ 36 с на переезд адреса и повторный сбой при возврате ноды; (2) **PostgreSQL реплицируется асинхронно**: 0 потерянных подтверждённых записей в этом прогоне не означает гарантии — при отказе primary записи, не дошедшие до реплики, теряются (`synchronous_mode` не включён, D8); (3) при падении `lb`-ноды до NotReady половина новых соединений к БД/Redis через мёртвый под HAProxy обрывается по таймауту (пулы соединений Harbor это переживают); (4) `docker pull` прячет часть ошибок за собственными повторами, а голый `curl` их показывает; (5) `s3` — единственная нода без резервирования (по D1).

### Phase 5 — Документация

- [x] **H5.1** `README.md`: HA-раздел, актуальные примеры вывода (2026-09-24). README переписан целиком: архитектура и поток запроса, требования с **измеренными** ресурсами (полный стенд ≈ 4,5 ГиБ RAM в покое, ≈ 8 ГБ образов; прежняя оценка 12–13 ГиБ была завышена), закреплённые версии, сборка (замеренное время), примеры реального вывода со стенда (ноды с ролями, поды по нодам, Consul, Patroni, Sentinel, HAProxy, Garage), таблица тестов `hack/tests/` (что делает и сколько идёт), таблица измеренных окон отказов, заметки по проекту, сгруппированные по темам (размещение и rollout, Redis, Harbor и TLS, хранилище и среда). Убраны устаревшие утверждения («поведение при отказах не проверялось», «CA пересоздаётся при каждой установке», «оценка не измерена», «полная пересборка ещё не выполнена»). Проверено: все упомянутые файлы и `make`-цели существуют, разделы ранбука V1–V12 на месте.
- [x] **H5.2** `AGENTS.md` / `CLAUDE.md` под фактическое состояние (2026-09-24). Разделены роли и убрано дублирование: `CLAUDE.md` (110 строк вместо 135) — правила, закреплённые версии, команды, ловушки по темам (сборка и хост; chart Harbor, TLS, rollout; Redis/HAProxy/S3; тесты отказов), связка входного пути, demo-приложение, соглашения; `AGENTS.md` (44 строки) — ориентация: куда смотреть, раскладка каталогов, умолчания, типовой порядок работы. Убрано устаревшее: раздел «Baseline (single-node)», «V1..V10», упоминания MinIO как текущего хранилища, «баз доступа HA не готов», старая схема связки на `harbor.yaml`. Сохранены правила исходных инструкций (порядок фаз, «спрашивать при новом решении», не выдумывать версии, сверять ключи chart, один лабораторный кластер) и добавлены правила, выученные в ходе работы: предупреждать перед разрушающим тестом, ждать спада нагрузки, проверять доступность закреплённых образов перед удалением кластера, удалять артефакты по digest, не трогать `~/.aws`. Проверено: все пути из документов существуют (кроме генерируемого `hack/phase0-baseline.log`). `hack/config/harbor.yaml` помечен как устаревший (ни одна цель его не использует); решение об удалении файла — за пользователем.
- [x] **H5.3** Acceptance с нуля: `cluster-delete` → `cluster` → зависимости → `install` (HA) → `deploy-app` → проверки Phase 4.
  - **Прогресс (2026-09-24):** полная пересборка с нуля выполнена по шагам `make cluster-delete` → `make cluster` → `make infra-lb` → `make ha-deps` → `make harbor-ha` → `make deploy-app` (кластер + Infra LB ≈ 3 мин 53 с; `ha-deps` ≈ 4 мин без MinIO, Garage ≈ 28 с; Harbor ≈ 6 с + rollout; `deploy-app` ≈ 21 с). **Найдено:** образы MinIO (`quay.io/minio/minio`, `quay.io/minio/mc`) стали закрытыми (`401 Requires authentication`), на Docker Hub их нет — закреплённый digest получить негде; по решению пользователя S3 заменён на **Garage v2.4.1** (D4a); смоук-тест Garage вне кластера: PUT/GET/LIST, multipart 60 МБ, range, delete, abort, изоляция ключа. Установка Harbor с нуля прошла с первой попытки (все четыре Secret создаёт установщик, стабильный CA, `preStop`, `matchLabelKeys`); jobservice без рестартов. Веха 1 переподтверждена (14/14 нод, размещение, Consul/Patroni/Redis/HAProxy, Harbor 2×4 + trivy, Garage, сквозной путь). Phase 4 переподтверждён на Garage: H4.1 — `hack/tests/h41-push-pull.sh` 7/7; H4.2 — убийство активного registry и core: клиент получил 502, повторил, `exit=0`, digest совпал; H4.3 — 2 прогона, 0 ошибок (173+139 манифестов, 79+65 блобов, 137+109 pull); H4.4 (`harbor-worker2`, с вытеснением) — 0 ошибок из 1049 манифестов, 484 блобов, 813 pull, NotReady через 49 с, вытеснение через 240 с, 2/2 через 42 с; H4.5 — оба варианта, 0 ошибок (217 и 140 запросов); H4.6 — 13/13 в двух прогонах. Итоговая полная приёмка после H4.7 — впереди (после H4.7 нужно повторить полную пересборку и все проверки: изменились `redis.yaml`, `haproxy.yaml`, `harbor-ha.yaml`).
  - **Побочные находки этой пересборки:** (1) тест H4.6 зависел от локального состояния docker (слои остаются в content store после `rmi`, blob-запросов к Harbor нет, кэш не создаётся) — «холодный pull» теперь делает `curl`-ом (индекс → манифест платформы → блобы), digest индекса считается по телу; (2) Docker Hub временно давал `TLS handshake timeout` (нестабильный внешний доступ) — тест повторён; (3) liveness-проба Garage на RPC-порту 3901 засоряла лог `Handshake error` каждые 10 с — перенесена на порт S3 3900; (4) Garage показывает незавершённые multipart-загрузки после убитых подов (≈ 31 МБ после двух убийств); (5) при отладке по ошибке моей команды `aws configure set` в `~/.aws/config` пользователя были записаны параметры multipart — исправлено сразу (файл возвращён к исходным трём строкам), дальше для S3-проверок используется изолированная конфигурация.
  - **Итоговая приёмка с нуля (2026-09-24, после H4.7):** перед удалением кластера проверено, что все закреплённые образы и чарты по-прежнему доступны анонимно (Garage, HAProxy, Consul, PostgreSQL, Valkey, python по digest; metallb/ingress-nginx/harbor charts; node image) — то, чего не хватило при истории с MinIO. Пересборка `cluster-delete` → `cluster` → `infra-lb` → `ha-deps` → `harbor-ha` → `deploy-app`: 8 мин 46 с (кластер + Infra LB, дольше обычного из-за загрузки образов), 4 мин 21 с (`ha-deps`, Garage включён), 5 с (`harbor-ha`) + запуск подов, 12 с (`deploy-app`); **без единого ручного вмешательства**. На свежей сборке проверено: `min-replicas-to-write 1`, Sentinel `down-after` 15000, liveness core/jobservice 5 с × 6, Redis стартует с `fresh=1` (master `redis-0` сразу), HAProxy: один UP на PG и Redis. **Веха 1** переподтверждена (14/14 нод, размещение, дублей нет, Consul/Patroni/Redis/HAProxy, Harbor 2×4 + trivy, Garage, сквозной путь, блобы в Garage, единственный PVC — trivy). **Phase 4 на чистой сборке:** H4.1 — 7/7 (проверка «обе реплики registry» переделана: считаются все запросы `/v2/`, а не только загрузки — две загрузки могут случайно попасть на одну реплику); H4.2 — registry и core: `502` → повтор → `exit=0`, digest совпал; H4.3 — 0 ошибок (150 манифестов, 68 блобов, 102 pull); H4.4 (`harbor-worker2`, с вытеснением) — NotReady через 54 с, вытеснение через 241 с, 2/2 через 40 с, ошибок 1 из 1153 манифестов / 521 блоба / 755 pull (один `502` за 0,2 с до убийства ноды, запрос «в полёте»); H4.5 — оба варианта, 0 ошибок (183 и 129 запросов); H4.6 — 13/13; H4.7 — consul (0 ошибок; писатель БД 13 ошибок в окне 2,7 с при выборе нового лидера raft, Patroni не переключался), redis (0 ошибок, зависания ≤ 21 с, 0 потерь, 0 откатов `INCR`, рестартов Harbor нет), pg (манифесты 113 из 428 и блобы 36 из 180 с 5xx в окне ≈ 16 с, pull 0 из 383, 0 потерянных из 554), lb (ошибки в первые ~45 с и при возврате ноды: манифесты 13 из 319, блобы 10 из 161, pull 0 из 255; 0 потерь). После серии отказов роли сменились сами (лидер PG `pg-1`, master Redis `redis-1`, лидер Consul `consul-2`), стенд здоров.
  - **Заметки этого прогона:** (1) рестарт контейнера jobservice в 15:56:24 — следствие временного сбоя DNS во время H4.6 (тест перезапускает CoreDNS; jobservice при старте не смог разрешить `harbor-core`), отчёт H4.7 относил его к тесту из-за дефекта моего учёта рестартов (два пода jobservice получали одинаковый ключ) — учёт переведён на полные имена подов; (2) потеря лидера Consul может дать короткий (≈ 3 с) сбой записи в PostgreSQL, пока raft выбирает лидера (HAProxy на время считает primary недоступным); Patroni лидера не менял.
- [x] **H5.5** Кэш образов для пересборки (2026-09-24). `hack/images.txt` — единый список сторонних образов (19 шт.: node image kind, базы сборки python/postgres, MetalLB ×4 + certgen ingress-nginx, ingress-nginx, Consul, Valkey, HAProxy, Garage, шесть образов Harbor) с ролями нод, которым они нужны; `hack/image-cache.sh save|load|check|status` (цели `make images-save|images-load|images-check|images-status`); кэш вне репозитория: `$IMAGE_CACHE`, по умолчанию `~/.cache/harbor-ha` (≈ 4,2 ГБ: образы 4,0 ГБ + три чарта; решение о месте принято мной по умолчанию, меняется переменной). `save` идемпотентен (`FORCE=1` — заново), кладёт `helm pull` чартов metallb 0.16.1, ingress-nginx 4.15.1, harbor 1.19.2; `install-infra.sh` и `install-harbor-ha.sh` берут архив из кэша, если он есть. `load` кладёт образы **только в ноды своей роли** (`kind load image-archive --nodes`), не во все 14. **Механика** (проверено): `docker save` образа, полученного по digest, теряет имя (`RepoTags: null`) — kind импортирует его как `import-…@sha256`, и kubelet его не находит; поэтому перед сохранением ставится служебный тег `cache.local/<имя>:cached`, а после загрузки на ноде `ctr -n k8s.io images tag` переименовывает образ в закреплённое имя (`docker.io/<репо>@sha256:<digest>` или `<репо>:<тег>`); под с закреплённым digest стартует с событием «already present on machine». **Приёмка пересборкой:** `cluster-delete`, локальные копии образов удалены, затем `cluster` → `images-load` → `infra-lb` → `ha-deps` → `harbor-ha` → `deploy-app` → `make verify`: **38 PASS, 0 FAIL**; `infra-lb` 49 с (было минуты загрузок), `ha-deps` 2 мин 58 с (было 4 мин 21 с), загрузка образов в ноды 2 мин 26 с; в событиях `Pulling` только `kube-webhook-certgen` (пропущен в первом списке, добавлен). **Найдено:** (1) **скрытая гонка в Consul**: при быстром старте (образ уже на ноде) три пода стартовали одновременно, каждый находил через `-retry-join` собственную запись DNS (она публикуется до готовности) и считал вступление успешным, поэтому получились три отдельных кластера из одного узла без лидера; раньше медленная загрузка образов маскировала гонку. Исправлено: `hack/ha/consul.yaml` запускает агент через `sh -c` и передаёт `-retry-join` только на *другие* серверы; проверено холодным стартом (StatefulSet и PVC удалены, `make consul`): три пира, лидер выбран. (2) Docker (классическое хранилище образов) не создаёт тег с digest для загруженного образа, поэтому `images-load` для роли `host` ставит тег без digest (не поверх существующего), а `make cluster` использует такой локальный образ, если образа с digest нет (`node image … not in Docker, using the cached …`); сборки `FROM python:3-alpine@sha256…` и `FROM postgres@sha256…` (Patroni) без сети по digest не получат базу — остаточный риск (официальные образы Docker Hub, малы). Не кэшируются пакеты PyPI образа Patroni (`make pg-image`, закреплены `constraints.txt`) и образ demo-приложения (в Harbor). (3, уточнено 2026-09-25: после правки проверка MISSING/UNKNOWN разделяет ответ реестра «нет/запрещено» и сетевую ошибку; при проверке всех целей `make` найдена и исправлена моя ошибка в `probe`, из-за которой успешные проверки печатались как MISSING) `docker manifest inspect` (`images-check`) в этой сети нестабилен: ответы приходят с таймаутами и результат «MISSING» менялся между прогонами; теперь таймаут (90 с, один повтор) выводится как `UNKNOWN`, а не как отсутствие образа. (4) Docker Hub на этом хосте отдаёт образы Harbor медленно (около 8 минут на образ, `make images-save` первый раз шёл 57 минут): смысл кэша ещё и в этом. Не проверено сквозным прогоном: ветка `make cluster` без сети с образом node из кэша (проверены логика, `make -n` и загрузка образа `host` на тестовом образе).
- [x] **H5.4** Ранбук переведён в Ansible: `ansible/verify.yml` (2026-09-24). Роль на раздел (`verify_cluster` … `verify_load` = V1..V12), теги `V1`..`V12`, `kubernetes.core` (`k8s_info`, `k8s_exec`, `k8s`, `k8s_log`) + `uri`/`command` для хоста; числа и адреса в `ansible/group_vars/all.yml`; каждая проверка записывается как `PASS`/`FAIL`/`WARN`/`INFO`, сводная таблица в конце, **ненулевой код возврата при любом `FAIL`**; запуск `make verify` (`TAGS=V5,V6`, `EXTRA='-e verify_rollout=true'`, `-e verify_writes=false`). Разрушающие сценарии P4.x остались скриптами `hack/tests/`. **Результат на живом стенде:** 37 `PASS`, 0 `FAIL`, 6 `WARN`/`INFO` за ≈ 1,5 мин; отрицательные проверки: `expected_nodes=15` → `FAIL V1.1` и код возврата 2; `kubectl scale deploy/harbor-portal --replicas=1` → `FAIL V11.1 bad: portal`, после возврата `2` снова зелёный; `V11.6` (`verify_rollout=true`) проходит. **Найдено в ходе работы:** (1) на хосте не было Python-модуля `kubernetes` (`pip install --user kubernetes`, 36.0.3), коллекция `kubernetes.core` 3.2.0 и `aws` CLI уже стояли; (2) Job проверки V9 в `default` не видит Secret'ов из `harbor-deps` (`CreateContainerConfigError`) — пароли копируются во временный Secret `verify-v9` и удаляются вместе с Job; (3) в `k8s_exec` команда разбирается через `shlex`, а не оболочкой: `\$VAR` не раскрывается в поде, нужно `$VAR` внутри `sh -c "..."`; (4) в отчёте V12.3 (число адресов клиентов на primary) в покое бывает 1 из 2, поэтому проверка даёт `WARN`, а не `FAIL`; (5) поды `Terminating` ещё числятся `Running` — без фильтра по `deletionTimestamp` проверка размещения давала ложный отказ сразу после rollout. Ограничение: `verify_reach` (V9) и проверки записи меняют состояние кластера (временный Job/Secret, таблица, ключ) и убирают за собой; при обрыве плейбука остатки: Job/Secret `verify-v9` в `default`, таблица `v54_probe`, ключ `v63`.

### Phase 6 — Оставшиеся проверки (добавлено 2026-09-25 по итогам H5.5)

- [x] **H6.1** Проверить offline-ветку `make cluster`: образ node kind берётся из кэша, когда образа с закреплённым digest в Docker нет (`make images-load` для роли `host`, затем `make cluster`); пересобрать стенд от кластера до `make verify` (2026-09-25). **Результат:** после `cluster-delete` удалены все формы образа node (digest, тег, служебный тег кэша), в Docker его не осталось; `make images-load` вернул `kindest/node:v1.34.0` из tar, `make cluster` вывел `node image … not in Docker, using the cached kindest/node:v1.34.0` и создал кластер за 42 с (с сетью — свыше 2 мин); далее `images-load` (нод) 3 мин, `infra-lb` 38 с, `ha-deps` 3 мин 15 с, `harbor-ha` 53 с вместе с запуском, `deploy-app`; всего **10 минут** от удаления до зелёного `make verify` — **38 PASS, 0 FAIL**. Единственные события `Pulling` — образ demo-приложения из самого Harbor. **Оговорка:** сеть физически не отключалась (без `sudo` нельзя изолировать демон Docker); проверено, что путь не использует образ по имени с digest и не обращается к реестру за образом node, а все образы нод — из кэша.
- [x] **H6.2** Как включить `synchronous_mode` в Patroni и что он меняет (2026-09-25); проверено на стенде скриптом `hack/tests/h62-sync-mode.sh <off|on|strict>` (писатель через `harbor-lb` пишет строку каждые ≈ 0,3 с; фаза 1 — удаляется под реплики, фаза 2 — под лидера; в конце конфигурация возвращается к исходной), **в стенд по умолчанию не включён** (D8, решение за пользователем).
  - **Как включить:** на работающем кластере `kubectl -n harbor-deps exec pg-0 -- patronictl -c /etc/patroni/patroni.yml edit-config --force -s synchronous_mode=true` (плюс `-s synchronous_mode_strict=true` для строгого режима); вернуть — `-s synchronous_mode= -s synchronous_mode_strict=`. Действует сразу, рестарт подов не нужен. Чтобы режим был при первичной инициализации нового кластера, добавить `synchronous_mode: true` в раздел `bootstrap.dcs` в `hack/ha/postgres.yaml` (рядом с `ttl`, `maximum_lag_on_failover`); для уже созданного кластера этот раздел не применяется, нужен `edit-config`. Признаки: в `patronictl list` роль реплики `Sync Standby`, на лидере `synchronous_standby_names = "pg-1"`.
  - **Измерено (удаление пода реплики, затем пода лидера; 0 потерянных подтверждённых строк во всех режимах):**

| Режим | Потеря реплики | Потеря лидера (graceful) |
|-------|----------------|--------------------------|
| `off` (как в стенде) | записи не прерываются (пауза ≤ 0,4 с) | пауза 11,0 с, 4 отказа записи |
| `on` | записи не прерываются (пауза ≤ 0,3 с): Patroni снимает sync-реплику и продолжает асинхронно | пауза 6,9 с, 3 отказа |
| `strict` | **записи блокируются на ≈ 15,6 с** — пока реплика не вернулась | пауза 15,4 с, 2 отказа, самая медленная запись 8,5 с |

  - **Что меняет:** (1) `off`: подтверждённая запись может не дойти до реплики; при жёстком отказе лидера повышенная реплика может её не иметь (допустимое отставание при выборе — `maximum_lag_on_failover` = 1 МиБ). (2) `on`: коммит ждёт подтверждения от sync-реплики, а при падении лидера Patroni повышает только sync-реплику, то есть подтверждённые записи сохраняются; **но** пока sync-реплики нет, кластер сам откатывается в асинхронный режим и гарантия временно пропадает. (3) `strict`: гарантия без исключений (без реплики запись блокируется, Harbor не может писать в БД, пока реплика не вернулась). Накладные расходы `on` на этом стенде не видны (запись < 0,05 с).
  - **Что изменится в тестах:** H4.7 `pg` — строка «0 потерь в этом прогоне, гарантии нет» станет «гарантия есть, пока жива sync-реплика» (`on`) или «всегда» (`strict`); окно записи при потере лидера остаётся 16–33 с (определяется `ttl` 30/`loop_wait` 10, sync его не сокращает); в режиме `strict` появляется новый сценарий — потеря **реплики** останавливает запись (`h47 pg` нужно дополнить, а liveness core/jobservice ≈ 60 с (H4.7) может не хватить при длительной потере реплики); H4.4 (нода `app`), H4.1–H4.3, H4.5, H4.6, H5.3, `make verify` не меняются (V5.1/V5.2 ожидают роль `Replica`, при `on` она станет `Sync Standby`: проверку V5.1 нужно будет расширить).
  - **Ограничение измерения:** удаление пода — штатное выключение, при котором Patroni успевает выполнить корректную передачу; потерю подтверждённых записей при жёстком отказе (`docker kill` ноды) этот скрипт не воспроизводит — там результат H4.7 (0 потерь в одном прогоне, гарантии нет).
  - **Рекомендация:** если для прода важна сохранность подтверждённых записей — `synchronous_mode: true` (не strict): даёт гарантию, пока есть реплика, и не останавливает запись при её потере; `strict` — только если недоступность записи допустимее потери данных. Это решение пользователя, в стенде оставлено `off`.

## Риски и заметки

- 14 нод-контейнеров на одной машине: без Harbor измерено ≈ 3,9 ГиБ (см. «Аудит перед Phase 3»), со всем стендом оценка ≈ 12–13 ГБ при ≈ 27–30 ГБ доступных; при нехватке ресурсов сокращать по согласованию (например, число Consul-серверов), не молча.
- Один хост Docker: «HA» здесь учебная — общий диск, общее ядро, реальной изоляции отказов нет.
- (Устарело, исправлено в H3.x) CA Harbor раньше пересоздавался при каждой установке; теперь он стабилен (Secret `harbor-ha-ingress-tls`), `make deploy-app` ставит доверие на ноде один раз.
- Ресурсы: оценка оказалась завышенной; измерено ≈ 4,5–6 ГиБ RAM для полного стенда (H5.1, H5.3); диск ≈ 8 ГБ образов + кэш `~/.cache/harbor-ha` ≈ 4,2 ГБ.
- Уроки базовой линии, которые легко повторить: непинованные зависимости ломают сборку со временем (случай Flask/Werkzeug); MetalLB L2 не держит анонс сервиса без Ready-эндпоинтов — при «зависшем» LB IP сначала смотреть `kubectl get endpoints`.
